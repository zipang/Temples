---
description: Drive development with tests (red first, then green)
---
Invoke the `test-driven-development` skill for: $ARGUMENTS

Write a failing test first, then make it pass. For bug fixes, reproduce with a test before attempting a fix. Prove the behavior works.
