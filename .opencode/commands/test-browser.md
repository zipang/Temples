---
description: Test/debug real web pages & web components in the browser
---
First, use `bun serve --hot` to launch a local page with hot module reload enabled.
Then use the `agent-browser` tool (load the skill) to access the page content and debug in  real time our scripts / web components or even style.
