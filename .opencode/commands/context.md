---
description: Load the right agent context
---
Invoke the `context-engineering` skill for: $ARGUMENTS

Curate what the agent sees — the right context at the right time — based on the current session/task. Configure rules/context to improve agent output quality.
